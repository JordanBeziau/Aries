{% extends "base.html.twig" %}
{% block body %}
  <div class="container">
    <div class="row justify-content">
      <h1 class="col-lg-9">{{ todo.title }}</h1>
      <p class="col-lg-3 line-height">{{ todo.category.name }} - {{ todo.createdAt | date("d/m/Y") }}</p>
    </div>
    <hr>
    <p>{{ todo.content }}</p>
  </div>
{% endblock %}